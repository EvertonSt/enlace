{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @enlace/core build && pnpm --filter @enlace/web build",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
